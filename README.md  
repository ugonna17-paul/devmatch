# DevMatch

DevMatch is a graph-based developer discovery platform built with React, Node.js, Express, and Neo4j.

It connects developers, skills, and projects to make it easy to discover relevant developers and understand their experience.

## Features

- Browse all developers
- View detailed developer profiles
- Browse projects and connected developers
- Browse available skills
- Find developers by skill
- Discover connected developers through projects and skills
- View project details, skills, and developers

## Tech Stack

### Frontend
- React
- Vite
- Tailwind CSS
- Axios

### Backend
- Node.js
- Express.js
- Neo4j
- Cypher

## Graph Model

```text
Developer ── HAS_SKILL ──> Skill
Developer ── WORKED_ON ──> Project
Project ── USES_SKILL ──> Skill
```

The graph allows developers to be connected through shared project and skill relationships.

## Key Graph Query

```cypher
MATCH (developer1:Developer)-[:WORKED_ON]->(project:Project)
      -[:USES_SKILL]->(skill:Skill)
      <-[:HAS_SKILL]-(developer2:Developer)
WHERE developer1.name < developer2.name
RETURN DISTINCT developer1, developer2, project, skill
ORDER BY developer1.name
```

This query finds developers connected through a project and a shared skill.

## API Endpoints

### Developers
- `GET /api/developers`
- `GET /api/developers/by-skill?skill=React`
- `GET /api/developers/by-project-skill?skill=React`
- `GET /api/developers/connections`
- `GET /api/developers/profile/:name`

### Projects
- `GET /api/projects`
- `GET /api/projects/by-name?name=DevMatch`

### Skills
- `GET /api/skills`

## Running the Project

### Backend

```bash
cd backend
npm install
npm run dev
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Environment Variables

Create a `.env` file inside `backend`:

```env
COGNODB_URI=your_neo4j_uri
COGNODB_USERNAME=your_neo4j_username
COGNODB_PASSWORD=your_neo4j_password
```

Do not commit the `.env` file.

## Database Seeding

To populate the Neo4j database with the sample developers, projects, skills, and relationships:

```bash
node src/seed/seed.js
```

## Project Structure

```text
devmatch/
├── backend/
│   ├── node_modules/
│   ├── src/
│   │   ├── config/
│   │   ├── controllers/
│   │   ├── queries/
│   │   ├── routes/
│   │   └── scripts/
│   ├── .env
│   ├── .gitignore
│   ├── index.js
│   ├── package-lock.json
│   └── package.json
│
├── frontend/
│   ├── node_modules/
│   ├── public/
│   ├── src/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   ├── App.css
│   │   ├── App.jsx
│   │   ├── index.css
│   │   └── main.jsx
│   ├── .gitignore
│   ├── eslint.config.js
│   ├── index.html
│   ├── package-lock.json
│   ├── package.json
│
├── README.md   
```

## Author

Built as a graph-based developer discovery application demonstrating Neo4j relationships, Cypher queries, REST APIs, and a React frontend.